# 综合评分体系实施说明

## 已完成的修改

### 后端修改

#### 1. 新增综合评分函数 (`backend/app/services/limit_board_scoring.py`)

新增了 `comprehensive_score()` 函数，实现100分制综合评分：

**评分结构：**
- **历史基因 (40分)**
  - 次日收红率 (15分)
  - 封板成功率 (15分)
  - 连板能力 (10分)

- **当日质量 (30分)**
  - 盘中走势 (15分) - 基于 intraday_flow 的趋势评分
  - 资金情绪 (10分) - 基于资金流向
  - 炸板风险 (5分) - 基于炸板次数

- **技术形态 (30分)**
  - 技术指标 (15分) - MA、MACD、RSI等
  - 板块位置 (10分) - 板块强度和个股排名
  - 量价配合 (5分) - 量比与价格动量

**评级标准：**
- A+ (85-100分): 优秀
- A (75-84分): 良好
- B+ (65-74分): 中上
- B (55-64分): 中等
- C (45-54分): 一般
- D (<45分): 较差

**输出信息：**
- 综合评分和评级
- 三个维度的详细得分
- 自动识别的优势和警示信息

#### 2. 集成到候选评分流程 (`backend/app/services/limit_board_service.py`)

- 导入 `comprehensive_score` 函数
- 在构建 `candidate_score_detail` 时自动计算综合评分
- 将综合评分添加到 `detail["comprehensive"]` 字段

### 前端修改

#### 1. 类型定义 (`frontend/src/lib/api.ts`)

在 `LimitBoardRow` 类型的 `candidate_score_detail` 中新增：
```typescript
comprehensive?: {
  comprehensive_score: number
  max_score: number
  grade: string
  grade_label: string
  dimensions: { ... }
  warnings?: string[]
  strengths?: string[]
  detail_available?: { ... }
}
```

#### 2. 新增综合评分展示组件 (`frontend/src/components/ComprehensiveScore.tsx`)

创建了独立的 `ComprehensiveScore` 组件，特性：
- 大卡片展示总分和评级
- 进度条可视化
- 自动高亮优势和警示
- 三个维度的详细评分卡片
- 支持紧凑模式（用于买入池）

#### 3. 更新确认对话框 (`frontend/src/pages/LimitBoard.tsx`)

- 导入 `ComprehensiveScore` 组件
- 在打板池确认对话框中优先展示综合评分
- 在买入池确认对话框中也添加综合评分（紧凑模式）
- 保留原有涨停基因展示作为后备方案

## 测试验证

### 后端测试

1. **启动后端服务**
```bash
cd backend
python -m app.main
```

2. **验证评分计算**
- 打开短线猎手页面
- 查看打板池或买入池中的标的
- 检查后端日志，确认 `comprehensive_score` 函数被调用
- 使用浏览器开发者工具查看 API 响应，确认返回了 `comprehensive` 字段

### 前端测试

1. **启动前端开发服务器**
```bash
cd frontend
npm run dev
```

2. **测试打板池确认对话框**
- 进入"短线猎手" -> "打板池"
- 点击任意标的的"设置该股票的交易数量或金额"按钮
- 验证显示：
  - ✓ 综合评分大卡片（总分、评级、进度条）
  - ✓ 优势和警示标签
  - ✓ 三个维度的评分卡片
  - ✓ 每个维度的细分指标

3. **测试买入池确认对话框**
- 进入"短线猎手" -> "买入池"
- 点击"确认加入买入池"
- 验证显示紧凑版综合评分（隐藏细分指标）

4. **后备方案测试**
- 找一个没有综合评分数据的标的
- 验证是否正确降级显示原有的涨停基因信息

### UI视觉验证

检查以下视觉元素：
- [ ] 评级颜色正确（A+/A绿色、B+/B黄色、C/D红色）
- [ ] 进度条动画流畅
- [ ] 优势标签使用绿色勾选图标
- [ ] 警示标签使用黄色警告图标
- [ ] 三个维度卡片布局均匀
- [ ] 字体大小适中，不拥挤

## 数据流向

```
后端评分计算流程：
1. limit_board_service.py 收集候选标的数据
2. 调用 premium_gene_detail() 计算历史基因
3. 调用 intraday_flow_detail() 计算盘中质量
4. 调用 technical_detail() 计算技术指标
5. 调用 sector_detail() 计算板块位置
6. 调用 comprehensive_score() 整合所有维度
7. 将结果添加到 candidate_score_detail.comprehensive
8. 通过 API 返回给前端

前端展示流程：
1. LimitBoard 组件接收 row.candidate_score_detail
2. 在确认对话框中检查 comprehensive 字段
3. 如果存在，渲染 ComprehensiveScore 组件
4. 否则降级显示原有的涨停基因信息
```

## 注意事项

### 性能考虑

1. **缓存机制**：综合评分会跟随原有的评分缓存机制，避免重复计算
2. **数据可用性**：如果某些维度数据缺失，评分会优雅降级（该维度得0分）
3. **计算效率**：综合评分函数非常轻量，主要是数学运算，不涉及IO操作

### 数据依赖

综合评分依赖以下数据源：
- `premium_gene`: 涨停基因（历史统计）
- `intraday_flow`: 盘中资金流向
- `technical`: 技术指标（MA、MACD、RSI等）
- `sector`: 板块数据
- `break_count`: 炸板次数（来自候选标的数据）

如果某些数据源不可用，对应维度得分为0，但不会影响其他维度。

### 评分权重调整

如需调整评分权重，修改 `comprehensive_score()` 函数中的分数分配：

```python
# 历史基因 (40分)
history_components = {
    "next_day_red": _clamp(next_day_red_rate) * 15.0,        # 调整此处
    "seal_success": _clamp(first_board_seal_rate) * 15.0,    # 调整此处
    "consecutive_ability": _clamp(consecutive_rate) * 10.0,  # 调整此处
}

# 当日质量 (30分)
quality_components = {
    "intraday_trend": (trend_score / trend_max) * 15.0,      # 调整此处
    "capital_sentiment": (capital_score / capital_max) * 10.0, # 调整此处
    "break_risk": ...,  # 根据炸板次数赋值                   # 调整此处
}

# 技术形态 (30分)
technical_components = {
    "technical_indicators": (tech_score / tech_max) * 15.0,  # 调整此处
    "sector_position": (sector_score / sector_max) * 10.0,   # 调整此处
    "volume_price": ...,  # 根据量价配合赋值                 # 调整此处
}
```

## 后续优化建议

1. **历史回测**：使用历史数据验证评分体系的预测能力
2. **用户反馈**：收集实际交易反馈，优化权重分配
3. **可配置权重**：允许用户自定义各维度权重
4. **评分趋势**：展示标的评分的历史变化趋势
5. **批量对比**：提供多个标的的评分对比视图

## 回滚方案

如果需要回滚到原有的涨停基因展示：

1. **前端回滚**：删除或注释掉综合评分的条件判断，保留原有的涨停基因展示
2. **后端回滚**：注释掉 `comprehensive_score()` 的调用，不影响其他功能
3. **部分回滚**：保留后端计算，仅前端切换回旧UI

前端降级逻辑已内置，即使后端不返回综合评分，前端也会自动显示涨停基因信息。

## 文件清单

### 新增文件
- `frontend/src/components/ComprehensiveScore.tsx` - 综合评分展示组件

### 修改文件
- `backend/app/services/limit_board_scoring.py` - 新增综合评分函数
- `backend/app/services/limit_board_service.py` - 集成综合评分计算
- `frontend/src/lib/api.ts` - 新增类型定义
- `frontend/src/pages/LimitBoard.tsx` - 更新确认对话框UI

### 文档文件
- `打板池买入池增强打分方案.md` - 方案设计文档
- `综合评分实施说明.md` - 本文档
