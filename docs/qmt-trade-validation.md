# QMT 交易控件验证记录

更新时间：2026-08-29

## 快捷金额与比例

- 快捷金额和快捷比例分别占一行。
- 两行共用四列网格，四个入口逐列上下对齐。
- 快捷金额默认显示为下单按钮；点击铅笔图标后才进入编辑态，点击完成图标恢复为按钮。
- 当前默认金额为 `10,000`、`20,000`、`30,000`、`40,000`。
- 当前比例为 `16.7%`、`20%`、`25%`、`100%`。

## 融资可买

QMT 账户级 `fin_enbuy_balance` 在部分 Big QMT 版本会返回 `DBL_MAX` 占位值，不能直接作为页面金额。打板池和交易面板现在使用个股级 `query_credit_opvolume` 预览结果，并将其作为“该股票最大可买金额”展示。

实测状态：

- QMT 状态：`ready`
- 账户：`97202688`（信用账户）
- 标的：`002354.SZ`
- 参考价格：`7.85`
- 个股最大可买数量：`43,100` 股
- 个股最大可买金额：`338,335.00` 元
- 预览数量：`10,700` 股
- 预览委托金额：`83,995.00` 元

## 验证方式

- 页面：内置浏览器打开 `http://127.0.0.1:3018/limit-board`，检查快捷金额/比例的列对齐及融资可买展示。
- 构建：在 `frontend` 目录执行 `pnpm build`，通过 TypeScript 检查和 Vite 构建；仅保留既有 chunk size warning。
- 交易安全：本次只调用 QMT 预览接口，没有执行真实下单。
