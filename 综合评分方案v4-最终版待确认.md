# 打板池综合评分方案 v4（最终版）

## 适用场景

### 打板池
标的已经涨停或接近涨停，需要快速决策是否打板。
- ✅ 显示综合评分
- ✅ 三个维度详细评分

### 买入池
提前布局，等待涨停。
- ❌ 不显示任何评分
- ✅ 只显示价格和金额设置

---

## 打板池综合评分体系（100分制）

```
综合评分（100分）
├─ 历史涨停基因（40分）
│  ├─ 次日收红率 (15分)
│  ├─ 封板成功率 (15分)
│  └─ 连板能力 (10分)
│
├─ 板块情绪周期（30分）
│  ├─ 板块K线形态 (15分) - 5日涨跌幅、斜率、排名变化
│  ├─ 板块内位置 (10分) - 龙头/前排/跟风
│  └─ 板块强度 (5分) - 当日涨跌幅
│
└─ 拉升健康度（30分）
   ├─ 分钟级量价 (10分) - 盘中量价配合
   ├─ 资金流向 (10分) - 主力资金净流入
   └─ 日K形态 (10分) - 均线排列、K线实体、上下影线
```

---

## 一、历史涨停基因（40分）

### 1. 次日收红率（15分）
**定义**：历史涨停后次日收阳的概率

**评分公式**：
```
得分 = 次日收红率 × 15
```

**数据来源**：`premium_gene.next_day_red_rate`

**评级**：
- ≥80%：优秀（12-15分）
- 60-80%：良好（9-12分）
- <60%：一般（<9分）

---

### 2. 封板成功率（15分）
**定义**：首板封板成功率

**评分公式**：
```
得分 = 首板封板率 × 15
```

**数据来源**：`premium_gene.first_board_seal_rate`

**评级**：
- ≥80%：封板坚决（12-15分）
- 60-80%：封板一般（9-12分）
- <60%：容易炸板（<9分）

---

### 3. 连板能力（10分）
**定义**：涨停后继续连板的概率

**评分公式**：
```
得分 = 连板率 × 10
```

**数据来源**：`premium_gene.consecutive_rate`

**评级**：
- ≥70%：连板能力强（7-10分）
- 40-70%：中等（4-7分）
- <40%：多为一日游（<4分）

---

## 二、板块情绪周期（30分）⭐核心维度

评估板块在市场情绪周期中的位置，判断是主线、上升、退潮还是震荡。

### 1. 板块K线形态（15分）

**评估维度**：
- 5日涨跌幅（复利收益率）
- 趋势斜率（5日均值变化）
- 排名变化（相对市场位置）
- Top20持续天数（强度持续性）

**评分逻辑**（参考现有 `rotation_detail` 逻辑）：

#### 1.1 5日复利涨幅（0-6分）
```
5日涨幅 10%以上 → 6分
5日涨幅 5-10% → 4-6分线性
5日涨幅 0-5% → 2-4分线性
5日涨幅 -5-0% → 0-2分线性
5日涨幅 <-5% → 0分
```

**计算方法**：
```python
compound = prod(1.0 + daily_return for daily_return in last_5_days) - 1.0
score = linear_scale(compound, -0.05, 0.10, 6.0)
```

#### 1.2 趋势斜率（0-4分）
```
斜率 >1% → 4分（加速上涨）
斜率 0-1% → 2-4分线性（平稳上涨）
斜率 -1%-0% → 1-2分（震荡）
斜率 <-1% → 0分（下跌）
```

**计算方法**：
```python
mean = sum(returns) / 5
slope = sum((i - 2) * (return - mean) for i, return in enumerate(returns)) / 10
score = linear_scale(slope, -0.01, 0.01, 4.0)
```

#### 1.3 排名变化（0-4分）
```
排名提升 >30% → 4分（快速走强）
排名提升 15-30% → 3-4分
排名提升 0-15% → 2-3分
排名下降 0-15% → 1-2分
排名下降 >15% → 0分（退潮）
```

**计算方法**：
```python
rank_change = percentile_today - percentile_5days_ago
score = linear_scale(rank_change, -0.30, 0.30, 4.0)
```

#### 1.4 持续性（0-1分）
```
Top20持续天数 / 5
```
5天都在前20%排名 → 满分

**综合判断板块状态**：
```python
if top_20_days >= 3 and percentile >= 0.8 and compound > 0:
    label = "主线"  # 持续强势
elif rank_change >= 0.15 and slope > 0:
    label = "上升"  # 快速走强
elif rank_change <= -0.15 and slope < 0:
    label = "退潮"  # 快速走弱
else:
    label = "震荡"  # 横盘整理
```

**数据来源**：`sector.rotation_score` 和相关字段

---

### 2. 板块内位置（10分）

**定义**：个股在板块内的排名位置

**评分规则**：
```
龙头（排名第1，且领先第2名≥1%） → 10分
前排（排名前3） → 7-9分
中游（排名前10） → 4-6分
跟风（排名10名后） → 0-3分
```

**计算方法**：
```python
leadership = "leader" if (rank == 1 and leader_gap <= 0.01)
leadership = "front" if (rank <= 3 or leader_gap <= 0.01)
leadership = "follower"  # 其他

龙头 → 10分
前排 → 5分
跟风 → 0分
```

**数据来源**：`sector.leadership`, `sector.stock_rank`, `sector.leader_gap_pct`

---

### 3. 板块强度（5分）

**定义**：板块当日涨跌幅

**评分规则**：
```
涨幅 ≥4% → 5分
涨幅 2-4% → 3-5分线性
涨幅 0-2% → 1-3分线性
涨幅 <0% → 0分
```

**数据来源**：`sector.change_pct` 或 `sector.realtime_change_pct`

---

## 三、拉升健康度（30分）

评估拉升过程是否健康，包括分时量价配合和日K形态。

### 1. 分钟级量价（10分）

#### 1.1 价格走势（0-5分）
**定义**：盘中相对昨收的涨幅和水上时间

```
涨幅 ≥5% → 4分
涨幅 2-5% → 2-4分线性
涨幅 0-2% → 0-2分线性
```

加上：
```
水上占比 100%（完全水上）→ +1分
水上占比 80-100% → +0.8分
水上占比 <80% → 按比例
```

**数据来源**：`intraday_flow.trend_score` 或计算
- `trend_pct`：当前涨幅
- `underwater_ratio`：水下时间占比

#### 1.2 量价配合（0-5分）
**定义**：价涨量增的配合度

```
涨幅>0 且 成交量增长>0 → 5分
涨幅>0 且 成交量增长 -20-0% → 3分
涨幅>0 且 成交量缩量严重 → 1分
其他 → 0分
```

**计算方法**：
```python
amount_growth = recent_amount / earlier_amount - 1
if trend_pct > 0 and amount_growth > 0:
    score = linear_scale(amount_growth, -0.20, 0.50, 5.0)
```

**数据来源**：`intraday_flow.amount_growth`, `trend_pct`

---

### 2. 资金流向（10分）

**定义**：主力资金净流入情况

**评分规则**：
```
净流入 ≥30% → 10分
净流入 10-30% → 7-10分线性
净流入 -10-10% → 3-7分线性
净流出 <-10% → 0-3分线性
```

**计算方法**：
```python
net_flow_ratio = buy_ratio - sell_ratio  # 主动买入比例 - 主动卖出比例
score = linear_scale(net_flow_ratio, -0.60, 0.60, 10.0)
```

**数据来源**：`intraday_flow.capital_score` 或
- `net_flow_ratio`
- `buy_ratio` / `sell_ratio`

---

### 3. 日K形态（10分）

评估日K线的健康程度。

#### 3.1 均线排列（0-5分）
```
完美多头（价格>MA5>MA10>MA20>MA60） → 5分
强多头（价格>MA5>MA10>MA20） → 4分
中多头（价格>MA5>MA10） → 3分
弱多头（价格>MA5） → 2分
均线纠缠或空头 → 0分
```

**数据来源**：`technical.price`, `ma5`, `ma10`, `ma20`, `ma60`

#### 3.2 K线实体（0-3分）
**定义**：实体占整根K线的比例（反映多空力量）

```
实体占比 >70%（大阳线）→ 3分
实体占比 50-70%（中阳线）→ 2分
实体占比 30-50%（小阳线）→ 1分
实体占比 <30%（十字星/阴线）→ 0分
```

**计算方法**：
```python
body = abs(close - open)
total_range = high - low
body_ratio = body / total_range if total_range > 0 else 0
score = linear_scale(body_ratio, 0.3, 0.7, 3.0)
```

**数据来源**：日K线 `open`, `close`, `high`, `low`

#### 3.3 影线分析（0-2分）
**定义**：上下影线反映的压力和支撑

```
下影线长（>实体2倍）且上影线短 → +2分（强支撑）
上下影线均衡 → +1分（正常）
上影线长（>实体2倍）→ 0分（上方压力大）
```

**计算方法**：
```python
upper_shadow = high - max(close, open)
lower_shadow = min(close, open) - low
body = abs(close - open)

if lower_shadow > body * 2 and upper_shadow < body * 0.5:
    score = 2  # 锤子线，强支撑
elif upper_shadow > body * 2:
    score = 0  # 上影线长，压力大
else:
    score = 1  # 正常
```

**数据来源**：日K线 `open`, `close`, `high`, `low`

---

## 评级标准

| 评级 | 分数区间 | 标签 | 说明 |
|-----|---------|------|------|
| S | 90-100 | 完美 | 主线龙头，强势板块，拉升健康 |
| A+ | 85-89 | 优秀 | 各维度优异，值得重点关注 |
| A | 75-84 | 良好 | 整体不错，可以参与 |
| B+ | 65-74 | 中上 | 有一定机会，谨慎参与 |
| B | 55-64 | 中等 | 表现平平，观望 |
| C | 45-54 | 一般 | 不建议 |
| D | <45 | 较差 | 避开 |

---

## 智能警示和优势

### 自动警示
- 次日收红率 < 60%：「次日收红率偏低」
- 板块状态 = "退潮"：「板块退潮中」
- 板块排名 < Top10：「非板块龙头」
- 资金净流出 < -10%：「主力资金流出」
- 均线空头排列：「均线压制」
- 上影线过长：「上方压力大」

### 自动优势
- 次日收红率 ≥ 80%：「次日收红率高」
- 连板能力 ≥ 70%：「连板能力强」
- 板块状态 = "主线"：「主线板块龙头」
- 板块状态 = "上升"：「板块快速走强」
- 板块排名第1：「板块绝对龙头」
- 资金净流入 > 30%：「主力大幅流入」
- 完美多头排列：「完美多头排列」
- 大阳线实体 > 70%：「放量大阳线」

---

## 数据来源汇总

| 评分项 | 主要数据来源 | 备用数据来源 |
|-------|------------|------------|
| 次日收红率 | `premium_gene.next_day_red_rate` | - |
| 封板成功率 | `premium_gene.first_board_seal_rate` | - |
| 连板能力 | `premium_gene.consecutive_rate` | - |
| 板块K线形态 | `sector.rotation_score`, `days` | - |
| 板块内位置 | `sector.leadership`, `stock_rank` | - |
| 板块强度 | `sector.realtime_change_pct` | `sector.change_pct` |
| 分钟级量价 | `intraday_flow.trend_score`, `amount_growth` | - |
| 资金流向 | `intraday_flow.capital_score`, `net_flow_ratio` | - |
| 均线排列 | `technical.price`, `ma5/10/20/60` | - |
| K线实体 | 日K `open/close/high/low` | - |
| 影线分析 | 日K `open/close/high/low` | - |

---

## UI展示

### 打板池 - 完整版

```
┌──────────── 确认加入打板池 ────────────┐
│ 金健米业 600127.SH                     │
│                                        │
│ 【价格信息】                           │
│ 当前限价 11.770 | 预计委托 --          │
│                                        │
│ ┌─────────── 综合评分 ───────────┐    │
│ │          82.5 / 100             │    │
│ │            A 级                  │    │
│ │            良好                  │    │
│ │  ████████████████░░░░ 82.5%     │    │
│ └─────────────────────────────────┘    │
│                                        │
│ ✓ 主线板块龙头  ✓ 主力大幅流入         │
│ ⚠️ 次日收红率偏低                       │
│                                        │
│ ┌─ 历史涨停基因 (30.8/40) ──────┐    │
│ │ 次日收红  9.0 / 15  (60%)       │    │
│ │ 封板成功 13.5 / 15  (90%)       │    │
│ │ 连板能力  8.3 / 10  (83%)       │    │
│ │ ███████████████░ 77.0%          │    │
│ └─────────────────────────────────┘    │
│                                        │
│ ┌─ 板块情绪周期 (28.5/30) ──────┐    │
│ │ 板块形态 14.5 / 15  (主线)      │    │
│ │ 板块位置 10.0 / 10  (龙头)      │    │
│ │ 板块强度  4.0 / 5   (4.2%)      │    │
│ │ █████████████████░ 95.0%        │    │
│ └─────────────────────────────────┘    │
│                                        │
│ ┌─ 拉升健康度 (23.2/30) ────────┐    │
│ │ 分钟量价  8.5 / 10              │    │
│ │ 资金流向  9.2 / 10  (净流入32%) │    │
│ │ 日K形态   5.5 / 10              │    │
│ │ ██████████████░░ 77.3%          │    │
│ └─────────────────────────────────┘    │
│                                        │
│ 【交易设置】                           │
│ [金额配置...]                          │
│                                        │
│          [取消]  [确认加入打板池]      │
└────────────────────────────────────────┘
```

### 买入池 - 无评分

```
┌──────────── 确认加入买入池 ────────────┐
│ 金健米业 600127.SH                     │
│                                        │
│ 【价格信息】                           │
│ 当前限价 11.450 | 预计委托 5000元      │
│                                        │
│ 【交易设置】                           │
│ [金额配置...]                          │
│                                        │
│ ⚠️ 确认后立即按当前 TickFlow 价格发送   │
│    限价买入委托，委托结果以 QMT 与券   │
│    商回报为准。                        │
│                                        │
│          [取消]  [确认买入并挂单]      │
└────────────────────────────────────────┘
```

---

## 实施计划

### 阶段一：后端评分函数（已完成部分）
- [x] 历史涨停基因计算
- [ ] 板块情绪周期评分（利用现有 `sector` 数据）
- [ ] 拉升健康度评分（利用 `intraday_flow` + 日K计算）

### 阶段二：前端UI（已完成部分）
- [x] 综合评分组件
- [ ] 调整组件标签（板块情绪周期、拉升健康度）
- [ ] 买入池移除评分展示

### 阶段三：测试优化
- [ ] 验证评分合理性
- [ ] 调整权重和阈值
- [ ] 用户反馈优化

---

## 待确认事项

1. **评分权重**：
   - 历史涨停基因 40分
   - 板块情绪周期 30分
   - 拉升健康度 30分
   - 是否合理？

2. **板块状态判定**：
   - 主线：Top20持续3天以上，当前前20%，5日涨幅>0
   - 上升：排名提升>15%，斜率>0
   - 退潮：排名下降>15%，斜率<0
   - 震荡：其他
   - 是否准确？

3. **日K形态评分**：
   - 均线排列 5分
   - K线实体 3分
   - 影线分析 2分
   - 权重是否合理？

4. **买入池**：
   - 完全不显示评分，只显示价格和金额设置
   - 是否同意？

**请确认后我开始修改代码！**
