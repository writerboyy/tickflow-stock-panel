# 综合评分功能启动指南

## 问题诊断

您看到的确认对话框仍然显示旧版涨停基因，是因为：
1. 后端服务未运行（没有返回新的综合评分数据）
2. 前端可能需要重新加载以使用新组件

## 启动步骤

### 1. 启动后端服务

**在项目根目录执行：**

```bash
cd backend
python -m app.main
```

或者使用开发脚本（根据你的系统）：

```bash
# MacOS/Linux
./dev.sh

# Windows
.\dev.ps1
```

**验证后端启动：**
- 看到日志输出 "Application startup complete"
- 访问 http://localhost:8000/docs 应该能看到 API 文档

### 2. 重启前端开发服务器

**打开新终端，在项目根目录执行：**

```bash
cd frontend
npm run dev
```

**验证前端启动：**
- 看到 "Local: http://localhost:5173/"
- 浏览器自动打开或手动访问该地址

### 3. 测试综合评分功能

1. 登录系统
2. 进入"短线猎手"页面
3. 切换到"打板池"或"买入池"标签
4. 点击任意标的的设置按钮（齿轮图标）或"加入打板池"按钮
5. 在弹出的确认对话框中，应该看到：
   - 大卡片显示综合评分和评级（如 82.5分 A级）
   - 优势和警示标签
   - 三个维度的评分卡片（历史基因、当日质量、技术形态）

### 4. 调试步骤（如果仍然不显示）

#### 检查后端返回数据

打开浏览器开发者工具（F12）：
1. 切换到 Network 标签
2. 筛选 XHR 请求
3. 找到 `limit_board` 相关的 API 请求
4. 查看 Response，检查是否包含 `candidate_score_detail.comprehensive` 字段

**示例正确的返回数据：**
```json
{
  "candidate_score_detail": {
    "comprehensive": {
      "comprehensive_score": 82.5,
      "max_score": 100.0,
      "grade": "A",
      "grade_label": "良好",
      "dimensions": {
        "history": {
          "score": 32.5,
          "max_score": 40.0,
          ...
        },
        ...
      },
      "warnings": ["资金流向偏弱"],
      "strengths": ["次日收红率高", "连板能力强"]
    },
    "premium_gene": { ... },
    "intraday_flow": { ... },
    ...
  }
}
```

#### 检查前端控制台

查看 Console 标签是否有错误信息。

#### 强制刷新前端

如果前端缓存了旧代码：
- 按 Ctrl+Shift+R (Windows/Linux) 或 Cmd+Shift+R (Mac) 强制刷新
- 或在开发者工具中右键刷新按钮 → "清空缓存并硬性重新加载"

## 常见问题

### Q: 后端启动失败
**A:** 检查依赖是否安装完整：
```bash
cd backend
pip install -e .
```

### Q: 前端启动失败
**A:** 重新安装依赖：
```bash
cd frontend
rm -rf node_modules
npm install
```

### Q: 对话框仍然显示涨停基因
**A:** 可能的原因：
1. 后端未返回综合评分数据 → 检查后端日志和 API 响应
2. 数据源不完整（缺少必要的评分维度）→ 这是正常的降级行为
3. 前端缓存 → 强制刷新浏览器

### Q: 如何确认代码已更新？
**A:** 检查文件修改时间：
```bash
# 后端
ls -lh backend/app/services/limit_board_scoring.py
# 应该看到最新的修改时间

# 前端
ls -lh frontend/src/components/ComprehensiveScore.tsx
# 这个文件应该存在
```

## 验证清单

- [ ] 后端服务正在运行
- [ ] 前端开发服务器正在运行
- [ ] 浏览器已强制刷新
- [ ] API 返回包含 `comprehensive` 字段
- [ ] 控制台无错误信息
- [ ] 确认对话框显示综合评分卡片

## 回滚方案

如果遇到无法解决的问题，可以暂时回滚：

**前端回滚**（只需注释一行）：
```typescript
// 在 LimitBoard.tsx 中，将这行：
{(kind === 'board' || kind === 'buy') && row.candidate_score_detail?.comprehensive ? (

// 改为：
{false && row.candidate_score_detail?.comprehensive ? (
```

这样就会恢复到原有的涨停基因展示。

## 需要帮助？

如果按照以上步骤仍然无法看到综合评分：
1. 提供后端日志输出
2. 提供浏览器控制台错误信息
3. 提供 API 响应数据（去掉敏感信息）
