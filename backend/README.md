# TickFlow Stock Panel Backend

FastAPI backend for the TickFlow Stock Panel. It provides the application's
market data services, monitoring, strategy, backtesting, and API endpoints.

Run it from this directory with:

```bash
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 3018
```
